<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/jass.css">
    <link rel="stylesheet" href="/css/custom.css">
</head>
<body>
    <header class="display-flex justify-space-between align-center p-2 main-header">
        <h1>
            <a class="main-title" href="/">App Title</a>
        </h1>
        <nav>
            {{#if logged_in}}
            <a class="logout-btn main-links" type="button" id="logout">Logout</a>
            {{else}}
            <a class="main-links" href="/login">Login</a>
            <a class="main-links" href="/signup">Sign Up</a>
            {{/if}}
        </nav>
    </header>
    <main class="container container-fluid mt-5">
        
        {{{body}}}
    </main>

</body>
</html>

<script src="/js/logout.js"></script>